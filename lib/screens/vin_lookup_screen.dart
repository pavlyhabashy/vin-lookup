import 'package:flutter/material.dart';

class VinLookupScreen extends StatelessWidget {
  const VinLookupScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
